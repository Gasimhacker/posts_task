import 'package:flutter/material.dart';

const double KCircleAvatarRadius = 90;

const KWelcomeTextStyle = TextStyle(fontSize: 50, color: Colors.white);

const KThemeColor = Color(0xff14ADA7);
InputDecoration KTextFieldDecoration = InputDecoration(
    fillColor: Colors.white,
    filled: true,
    border: OutlineInputBorder(borderRadius: BorderRadius.circular(15)));
const KTextFieldPadding = EdgeInsets.all(15.0);
